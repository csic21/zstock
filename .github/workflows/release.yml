name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  windows:
    name: Windows (x64)
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2

      - name: Build release
        run: cargo build --release

      - name: Package
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force dist\stock-windows-x64 | Out-Null
          Copy-Item target\release\stock.exe dist\stock-windows-x64\
          Copy-Item README.md dist\stock-windows-x64\ -ErrorAction SilentlyContinue
          Compress-Archive -Path dist\stock-windows-x64 -DestinationPath dist\stock-analysis-windows-x64.zip -Force

      - name: Upload to release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/stock-analysis-windows-x64.zip
          generate_release_notes: true

      - name: Upload artifact (manual run)
        if: ${{ !startsWith(github.ref, 'refs/tags/') }}
        uses: actions/upload-artifact@v4
        with:
          name: stock-analysis-windows-x64
          path: dist/stock-analysis-windows-x64.zip

  macos:
    name: macOS (Universal)
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-apple-darwin, aarch64-apple-darwin

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2

      - name: Build release (x86_64 + arm64)
        run: |
          cargo build --release --target x86_64-apple-darwin
          cargo build --release --target aarch64-apple-darwin

      - name: Merge universal binary
        run: |
          mkdir -p target/release
          lipo -create -output target/release/stock \
            target/x86_64-apple-darwin/release/stock \
            target/aarch64-apple-darwin/release/stock

      - name: Package .app
        run: SKIP_CARGO_BUILD=1 ./scripts/package-macos.sh

      - name: Zip .app
        run: |
          cd dist
          ditto -c -k --keepParent "Stock Analysis.app" stock-analysis-macos-universal.zip

      - name: Upload to release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/stock-analysis-macos-universal.zip
          generate_release_notes: true

      - name: Upload artifact (manual run)
        if: ${{ !startsWith(github.ref, 'refs/tags/') }}
        uses: actions/upload-artifact@v4
        with:
          name: stock-analysis-macos-universal
          path: dist/stock-analysis-macos-universal.zip
